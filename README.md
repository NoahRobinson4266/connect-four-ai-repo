# connect-four-ai-repo
A playable Connect Four interface with custom UI, allowing the user to play locally against another human or against one of three AIs, which use either hard-coded rules or machine learning.
